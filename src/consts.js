const ProfileDBType = {
  CLIENT: 'client',
  CONTRACTOR: 'contractor'
}

const ProfileAssociationType = {
  CLIENT: 'Client',
  CONTRACTOR: 'Contractor'
}

const ContractStatus = {
  NEW: 'new', 
  IN_PROGRESS: 'in_progress',
  TERMINATED: 'terminated'
}

module.exports = {ProfileDBType, ProfileAssociationType, ContractStatus}